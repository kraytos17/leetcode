#include <vector>

class Solution {
public:
    auto minZeroArray(std::vector<int>& nums, std::vector<std::vector<int>>& queries) -> int {
        auto n = nums.size();
        auto sum = 0;
        auto k = 0;
        std::vector<int> diffArr(n + 1);
        for (auto i = 0uz; i < n; ++i) {
            
        }

        return k;
    }
};
